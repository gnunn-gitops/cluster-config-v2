apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: rhtpa-oidc
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: cluster
  target:
    name: rhtpa-oidc
  data:
    - secretKey: clientSecret
      remoteRef:
        key: RHTPA_OIDC
