apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
- oidc-external-secret.yaml

patches:
  - path: oidc-config-patch.yaml
  - patch: |
      - op: remove
        path: /spec/sso
    target:
      kind: ArgoCD
