apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

patches:
  - patch: |-
      - op: add
        path: /spec/defaultClusterScopedRoleDisabled
        value: true
    target:
      kind: Argo CD
